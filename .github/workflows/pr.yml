name: PR

on:
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - uses: cypress-io/github-action@v7.4.0
        with:
          build: npm run build
          start: npm start
